import { FC } from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";
import { IBaseScreen } from "../../types/screen";

const Groups: FC<IBaseScreen> = () => {
  return (
    <View>
      <Text>Groups works!</Text>
    </View>
  );
};

export default Groups;
